{
  "name": "mcp-windows-inspect",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "description": "MCP server for Windows window/control introspection (enumerate windows and child controls, read handle/class/title/rect/styles/control-id) — WinSpy-style, used to find the HWND to target with the other windows-window-* servers",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-windows-inspect": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && dotnet build native -c Release -o dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5"
  }
}

{
  "name": "mcp-windows-mouse",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "description": "MCP server that injects mouse events (move/click/drag/scroll) on Windows",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-windows-mouse": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && dotnet build native -c Release -o dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5"
  }
}

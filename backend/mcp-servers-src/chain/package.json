{
  "name": "mcp-windows-chain",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "description": "MCP server that runs a linear sequence of input/wait/process-lifecycle steps against the Windows desktop in a single tool call, instead of one LLM round-trip per step",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-windows-chain": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && dotnet build native -c Release -o dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5"
  }
}

{
  "name": "mcp-screen-video",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "description": "MCP server for recording the screen to video and sampling frames from it, so the model can see motion/dynamics, not just a single still screenshot",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-screen-video": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5"
  }
}

<Window x:Class="Caroline.AppBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Caroline Browser"
        Width="1100" Height="800"
        WindowStartupLocation="CenterScreen"
        Closing="OnClosing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#3A3F8F" LastChildFill="True">
            <TextBlock x:Name="UrlText" Foreground="#CCC" DockPanel.Dock="Right" VerticalAlignment="Center"
                       Margin="0,0,12,0" FontSize="11" TextTrimming="CharacterEllipsis" MaxWidth="380" />
            <TextBlock x:Name="TitleText" Foreground="White" VerticalAlignment="Center" Margin="12,0" FontSize="14" />
        </DockPanel>

        <Grid Grid.Row="1" x:Name="WebViewHost" Background="#222" />
    </Grid>
</Window>
